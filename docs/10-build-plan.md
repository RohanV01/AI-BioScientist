# Build Plan

Phased, each phase ending in something demonstrably working — not a phase boundary drawn for its own sake. Cross-referenced to the research report's Section 6 (Prioritized Shortlist) and Section 9 (Feasibility Check) so prioritization isn't invented fresh here. **See `11-backlog-traceability.md` for the full status of every one of the report's 105 experiments, 8 flagships, 10 gaps, and 13 paid integrations against these phases** — this document only names what's actually scheduled; it isn't a complete map of the report on its own.

## Architecture pivot (2026-08-15) — read before continuing past Phase 1

Phases 0 and 1 below were built and verified under the *original* design: one bot per research-report domain cluster (a "Literature Agent," a future "Drug Discovery Agent," etc.), each with its own narrow tool scope. **That's not the product** — see `07-system-architecture.md`'s pivot note for the full reasoning. The actual model: **one master orchestrator agent**, with the entire tool roster attached, that plans a methodology per query, executes across whatever tools that plan needs, and synthesizes a grounded report.

**What this means for what was already built:** nothing from Phase 0/1 was wasted, but Phase 2 now starts with a refactor, not new-agent work. `app/claude_runner.py`'s `run_literature_agent()` is domain-specific (hardcoded to the PubMed tool, a Literature-Agent-flavored system prompt) — it needs to become the general Plan→Execute→Synthesize runner, parameterized by *whatever tools are wired*, not one function per domain. The PubMed tool, the Grounding Layer, the isolation fix (`setting_sources=[]`), the ToolCall/GroundingLink persistence pattern — all of that is correct and reusable as-is.

## Phase 0 — Data audit & foundations (before any agent code)

**Goal:** de-risk the two biggest unknowns before building on top of them.

**Superseded (2026-08-15):** the original plan here was to bulk-import all of `scihub.sql` (~88M rows) into a queryable store and measure DOI-join coverage against `biology_dois.txt` upfront. Tested two parsing approaches (a hand-rolled character-state-machine and a regex-token-stream parser); both were too slow to be worth it (projected 6.5–21 hours for a one-time import). More importantly, it was solving the wrong problem: OpenAlex's live API already returns full metadata (title, abstract, year, journal, OA status) for any topic query, with no local parsing needed at all. Replaced with an **on-demand retrieval design** below — `scihub.sql` is now used only as a targeted, query-time Sci-Hub-availability lookup (confirmed via `grep -F -f` against the full 32.7GB file: ~50-60s per query, I/O-bound on one sequential read, and — because it's I/O-bound — checking one DOI or several hundred from one topic batch costs about the same single pass).

- [ ] **Literature Discovery & Acquisition design:** given a topic/question, (1) discover candidate DOIs + metadata by querying OpenAlex (public, unauthenticated) and PubMed MCP (live) in parallel — no local data needed for this step; (2) for the resulting DOI list, run a targeted `grep -F -f` lookup against `data/scihub.sql` to find which are Sci-Hub-available, extracting DOI/MD5/Filesize only for matches (reuses the row-parser already validated during the superseded approach, applied to the handful of matching lines, not the whole file); (3) full-text acquisition waterfall, in order: OA link (PMC/Unpaywall) → any BYO paywalled/publisher access configured for the org → Sci-Hub (via the MD5 found in step 2) as an explicit, allowed fallback — **user decision, 2026-08-15: Sci-Hub is in scope for the retrieval waterfall, not restricted to metadata-only.** This supersedes the original Gap 9 framing (see below). Still not implemented — becomes a tool in the master agent's roster (Phase 3), not its own agent.
- [x] Stand up Mattermost locally (docker-compose), confirm Bot Accounts + Outgoing Webhooks work with a trivial echo bot — fully done 2026-08-15. `scripts/bootstrap_mattermost.sh` creates admin/team/bot (idempotent, reusable by anyone cloning the repo). Outgoing Webhook end-to-end confirmed. **Note post-pivot: only one bot account is needed going forward** — the echo-bot/Literature-Agent bot becomes *the* bot, not one of several.
- [x] Scaffold the Orchestrator Service — done 2026-08-15. FastAPI skeleton (`orchestrator/app/`), all 9 tables from `06-data-model.md` as SQLAlchemy models (schema unchanged by the pivot — see `06-data-model.md`'s own pivot note), Alembic migration generated+applied+verified against a live Postgres, a Credential Vault module (Fernet encryption), and a Grounding Layer module that structurally enforces the `provenance_type` rule.
- [x] ~~Confirm RxDis still runs standalone post-move~~ — corrected 2026-08-15, then **executed 2026-08-15**: RxDis's code was recovered from git history (`git archive 542b56f src scripts tools testing requirements.txt docker-compose.yml Dockerfile | tar -x -C rxdis/`) into a new top-level `rxdis/` directory. Its `data/Databases`/Supabase path references still need updating before it runs (Phase 3 task, see below) — recovery alone doesn't make it runnable yet.
- [x] **Gap 9 compliance boundary — revised and documented.** Full-text sourcing is allowed to use Sci-Hub as an explicit fallback source, not restricted to OA-only. Requirement: every full-text response must **disclose which tier it came from** (OA / BYO-paywalled / Sci-Hub) — a provenance-labeling requirement, structurally supported via `provenance_type` + `citation_label`.

**Exit criterion — met 2026-08-15.** Phase 0 is complete.

## Phase 1 — First working agent run: Literature/PubMed (Tier 1 · research report Shortlist #9 pattern — shipped on zero new wiring)

**Built and verified under the pre-pivot design — the mechanics are correct and reused going forward; the "one agent per domain" framing they were built under is not.**

- [x] Wire PubMed MCP into the Orchestrator's Claude Code/Codex Runner — done 2026-08-15. Built as an in-process SDK MCP tool (`app/tools/pubmed.py`, real NCBI E-utilities calls) rather than a separate server process. Named `search_articles` to match the tool name already used throughout `docs/`. **This is now the first entry in the master agent's tool roster, not a standalone Literature Agent's only tool.**
- [x] Build the Grounding Layer's core enforcement — done in Phase 0 (`app/grounding.py`), proven for real in Phase 1: `ToolCall` rows persisted per real PubMed search, `GroundingLink` rows point at them, citation precision fixed to intersect what a tool actually returned with what the final answer actually discusses.
- [x] Journey 1 end-to-end, live — verified 2026-08-15 against the real running stack: a real question posted in Mattermost triggered the webhook, the Orchestrator ran a real Claude Code/Codex turn with real PubMed searches, and posted a `grounded` response with 10 real `GroundingLink` rows back into the channel. AC-1/AC-2/AC-4 all hold.
- [x] **Found and fixed a critical isolation bug:** `ClaudeAgentOptions(allowed_tools=[...])` alone does not sandbox a run. Without `setting_sources=[]`, the SDK loads the host's `~/.claude/settings.json` and every personal MCP connector configured there — a real test run used the developer's own personal PubMed/Gmail connectors instead of the intended tool, silently. Fixed; documented in `07-system-architecture.md` as a hard requirement for every agent run.
- [x] Fixed a second bug: `Task.status` was set to `"running"` at creation and never updated. Now transitions to `completed`/`failed`.

**Exit criterion — met 2026-08-15.** Real PubMed calls, real Claude Code/Codex tool use, real grounded response with verifiable citations traced through `Task → ToolCall → Response → GroundingLink`. **Known limitation:** proven on the host venv (inheriting the developer's authenticated `claude` CLI), not the Docker container — containerizing the CLI + non-interactive auth is a distinct packaging task, not yet done.

## Phase 2 — Refactor to the master-agent pattern (do this before adding any more tools)

**Goal:** turn Phase 1's domain-specific proof into the actual product shape — one agent, full roster, visible plan before execution.

- [x] Generalized `app/claude_runner.py` — done 2026-08-15. `run_literature_agent()` replaced with `run_agent(user_message, roster, on_plan)`, where `roster` is a `ToolRoster` built from the master `AGENT`'s `TOOL_BINDING` rows (`app/tool_roster.py`, new) instead of hardcoding PubMed. Growing the roster now means adding an entry to `TOOL_BUILDERS` + a DB row, not touching the runner.
- [x] Rewrote the system prompt to require the explicit **Plan → Execute → Synthesize** structure — done 2026-08-15, verified live: the agent posts "I'll search PubMed for review articles/studies on X..." as its own message *before* any tool call resolves, then executes, then posts the synthesized report separately.
- [x] The stated methodology posts as its own message in the thread, in real time (not bundled into the final report) — implemented via an `on_plan` callback the webhook handler passes into `run_agent()`, invoked the moment the first tool-calling turn starts.
- [x] Updated `app/routers/mattermost_webhook.py`: still resolves "the one active agent" (correct now, not a stub — there's genuinely only one), rewired to call `run_agent()` with a roster built per-request, and to persist `ToolCall` rows mapped back to the correct `ToolSource` via each call's MCP server name (`mcp__<server>__<tool>` → roster lookup) instead of assuming every tool call was PubMed.
- [x] Re-ran the PubMed question from Phase 1 against the refactored runner — same result shape (`grounded`, 10 real `GroundingLink` rows, 5 real `ToolCall` rows correctly attributed to the `pubmed` `ToolSource`), now via the general path.
- [x] Wired the missing `TOOL_BINDING` row (agent ↔ pubmed `ToolSource`) via an updated `scripts/seed_dev_data.py --tools pubmed` — this didn't exist before the refactor since Phase 1's code never read the roster from the DB.

**Exit criterion — met 2026-08-15.** Verified live against the real running stack: a real question ("what are the main resistance mechanisms to PARP inhibitors") produced a plan message posted first ("I'll search PubMed for review articles/studies on resistance mechanisms to PARP inhibitors..."), then a grounded final report with 10 real citations, entirely through the generalized path — no domain-specific code involved. **Known gap carried forward, not solved here:** citation extraction (`PMID (\d+)` regex in `claude_runner.py`) is still PubMed-shaped; it needs to generalize once Phase 3 adds tools with other record-ID formats (ChEMBL IDs, PDB IDs) — flagged in the code with a comment, tracked here so it isn't forgotten.

## Phase 3 — Grow the tool roster (Tier 1/2 mix) — following the research report's shortlist order

Not "add agents" anymore — each bullet is a tool source added to the one master agent's `TOOL_BINDING` roster. Order still follows the research report's Section 6 (Prioritized Build Shortlist) and Section 9 (Feasibility Check):

- [x] **ChEMBL** (Shortlist #3, partial — Open Targets still open) — done 2026-08-15. Built as a real in-process SDK tool (`app/tools/chembl.py`, `compound_search` + `get_bioactivity` against ChEMBL's free public REST API) — **not** wired via the developer's personal claude.ai ChEMBL connector, which is inaccessible to (and inappropriate for) an isolated agent run the same way the report's "already-live MCP" framing implied; every tool source in this platform's roster is a real standalone implementation, live connectors from the research/report-writing session don't carry over. **Found a second, more serious isolation leak while testing this:** even with Phase 2's `setting_sources=[]` fix, a live run silently used the developer's personal **account-linked** `claude_ai_ChEMBL` connector instead of the wired tool — a different mechanism than the Phase 1 leak (file-based settings vs. account-linked connectors). Fixed with `strict_mcp_config=True`, confirmed via a direct isolation test (only `mcp__chembl__*` tool names used, zero leak) and re-verified through the live Mattermost loop. Documented in `07-system-architecture.md` as a second, independently-required flag. Citation extraction also generalized in the same pass (`RECORD_REF_PATTERNS` registry in `claude_runner.py` — PubMed PMIDs + ChEMBL IDs both now recognized, extensible per new tool).
- [x] **Open Targets** (Shortlist #4) — done 2026-08-15. `app/tools/open_targets.py`: `search_entities` + `get_target_disease_associations` against Open Targets' free public GraphQL API, same in-process pattern as PubMed/ChEMBL. Initial version under-cited (the model's answer table listed disease names/scores but omitted the record ID needed to verify grounding, so the intersection-based citation check correctly excluded them — 1 citation instead of the ~16 it should've had). Fixed by tightening the master system prompt to require the record ID inline on every claim/table row, not just the first mention — re-verified, citation count went from 1 to 16 on the same query.
- [ ] **RxDis — architecture corrected 2026-08-16, not yet executed.** User decision: **RxDis is not wrapped as one monolithic tool source.** Instead, RxDis's own pipeline phases (target ID → validation → repurposing/de novo design → biologics → MPO → validation → packaging) each become their own separate, individually-callable tool the master agent can invoke — matching this platform's actual architecture (many small puzzle-piece tools, not a single black-box sub-agent that hides its steps behind one `trigger_run`/`poll_status` wrapper). This replaces the earlier plan (one `app/tools/rxdis.py` triggering a full pipeline run and mapping RxDis's own provenance into `GROUNDING_LINK` rows). **Still blocked on the same infra dependency regardless of wrapper shape** — RxDis needs its own running Supabase instance plus a heavy ML dependency set (PyTorch, RDKit, docking tools) before any of its phases can run standalone, and its `data/Databases`/Supabase path references are still broken since the `rxdis/` recovery in Phase 0. **User is still thinking through the phase-decomposition plan (which phases become which tools, how intermediate state passes between them) — do not start building this until they come back with direction.**
- [x] **Literature Discovery & Acquisition** — done 2026-08-15. `app/tools/literature_discovery.py`: `discover_papers` (OpenAlex works search, free/unauthenticated — DOI, title, year, subfield, and genuine OA status/URL per paper) + `check_scihub_availability` (given DOIs *not* already OA, `grep -F -x` against the flat one-DOI-per-line Sci-Hub index at `data/Databases/scihub/sci-hub-doi-2022-02-12.txt` — the ~88.3M-line plain-text list, not the 32.7GB `scihub.sql` dump; a few-DOI batch resolves in ~2-4s). Implements the OA → Sci-Hub acquisition waterfall from the Phase 0 design, with tier always disclosed inline (never presents a Sci-Hub hit as open access). New `SCIHUB_DOI_INDEX_PATH` setting in `app/config.py` / `.env.example`. **Bug found and fixed during live testing:** the grep subprocess, built with `asyncio.create_subprocess_exec`, worked perfectly in a standalone script but silently failed (non-0/1 returncode, empty stderr) when actually invoked as a tool inside the orchestrator's uvicorn process via the SDK's in-process tool dispatch — almost certainly a child-watcher/event-loop-thread mismatch specific to that runtime. Fixed by moving to a blocking `subprocess.run` wrapped in `asyncio.to_thread`, which sidesteps asyncio's subprocess machinery entirely; re-verified through the full live Mattermost loop (16 grounded citations, correct OA/Sci-Hub tier labeling, no isolation leaks). Also added a `DOI {}` pattern to `claude_runner.py`'s `RECORD_REF_PATTERNS`.
- [x] **Ensembl/UniProt/ClinVar/gnomAD + Ontologies** (Shortlist #2) — done 2026-08-15, split into one tool source at a time like everything else. **Ensembl's live re-verification closed the same day, once `rest.ensembl.org` partially recovered** — but the recovery was itself partial (the `xrefs/symbol` step succeeded consistently, `lookup/id` kept 500ing on 3 consecutive retries within the same run), which turned into a better verification than a clean run would have been: `tool_calls` show all 6 calls correctly attributed to `ensembl` (no isolation leak) even under repeated failure, and the agent handled the partial failure exactly as the system prompt requires — reported the one fact it actually confirmed (gene ID `ENSG00000146648`) as `grounded` (1 citation) rather than fabricating the location/description it couldn't retrieve, and said plainly that the rest failed after retrying. Confirms the grounding system holds under a real degraded dependency, not just the happy path. **UniProt: done and live-verified 2026-08-15** — `app/tools/uniprot.py`'s `search_protein` (free public REST, gene/name/free-text search → accession, names, organism, FUNCTION comment) proven through the full live Mattermost loop (grounded, 3 citations, correct `uniprot` attribution, no isolation leak). Added `UniProt ID {}` to `RECORD_REF_PATTERNS`. **Ensembl: code done, live verification blocked by an external outage** — `app/tools/ensembl.py`'s `search_gene` (symbol → stable Ensembl gene ID + coordinates + description, via `xrefs/symbol` then `lookup/id`) is written and wired into the roster, and worked via a direct `curl` test (`ENSG00000146648` for EGFR) before `rest.ensembl.org` started failing every request (500s then timeouts) for a sustained period the same session — confirmed not a code/isolation issue since UniProt (a different EBI-adjacent service) stayed up throughout. **Needs a live Mattermost re-verification once `rest.ensembl.org` recovers** before this bullet can be marked fully done. **ClinVar: done and live-verified 2026-08-15** — `app/tools/clinvar.py`'s `search_variants` (NCBI E-utilities against the `clinvar` db, same esearch/esummary pattern as `pubmed.py` — gene + optional free-text term → accession, name, germline classification, review status, condition) proven live (grounded, 20 citations across a real 20-variant BRCA1/pathogenic query, correct `clinvar` attribution, no isolation leak). Added `ClinVar ID {}` to `RECORD_REF_PATTERNS`. **gnomAD: done and live-verified 2026-08-15** — `app/tools/gnomad.py`'s `get_variant_frequency` (free public GraphQL API, chrom-pos-ref-alt variant ID → overall + per-continental-population allele frequency from exome/genome cohorts) is the population-genetics counterpart to ClinVar's clinical classification — pairing them is the natural rarity-supports-pathogenicity workflow. Live-verified (grounded, 1 correct citation, correct `gnomad` attribution, no isolation leak). Found and fixed one bug pre-live-test: gnomAD's GraphQL API returns "variant not found" as a top-level `errors` entry rather than a null `variant` field, which the first version treated as a hard failure instead of a normal empty result. Added `gnomAD variant {}` to `RECORD_REF_PATTERNS`. **Ontologies: done and live-verified 2026-08-15** — `app/tools/ontologies.py`'s `search_ontology_term`, built against EBI's Ontology Lookup Service (OLS, free/unauthenticated), one API covering Gene Ontology, HPO, MONDO, NCBI Taxonomy, ICD, and more rather than wiring each ontology separately — resolves free-text terms to normalized IDs (e.g. "seizure" → HP:0001250). **Found and fixed a real grounding bug on the first live test:** OLS returns ontology IDs in colon notation (`MONDO:0007254`, `HP:0001250`), but `RECORD_REF_PATTERNS`'s existing entry only matched the underscore notation Open Targets uses for the same identifiers (`MONDO_0007254`) — a fully tool-backed answer was silently mislabeled `synthesis` instead of `grounded` because of a notation mismatch, not a missing citation. Fixed by matching either separator (renamed the pattern `Ontology/Disease ID {}` since it now covers both tools); re-verified live, provenance flipped from `synthesis` (0 citations) to `grounded` (4 citations) on the identical query. **This closes out Shortlist #2's last new-code item** — only Ensembl's live re-verification remains open, and that's blocked on `rest.ensembl.org`'s outage recovering, not on anything in this codebase.
- [x] **KEGG/Reactome/STRING** (Shortlist #5) — done and live-verified 2026-08-15. `app/tools/kegg.py` (`get_gene_pathways`, flat-text REST — `rest.kegg.jp/get/hsa:<symbol>` accepts human gene symbols directly, no separate ID-resolution step needed), `app/tools/reactome.py` (`search_pathways`, Content Service REST — curated reaction-level pathway mechanism, the level of detail that distinguishes it from KEGG's higher-level diagrams), `app/tools/string_db.py` (`get_interaction_partners`, REST — protein-protein interaction network with a combined confidence score; module named `string_db` not `string` to avoid shadowing the stdlib). **Decision on the local-data-vs-live-API question this bullet flagged: went with STRING's live API**, same reasoning as every other tool source here — no local parsing/staleness to manage, consistent with the rest of the roster; the local STRING copy in `data/Databases/` remains unused. **Found the same "empty result returns 404, not an empty list" bug in both Reactome and STRING** (unrelated APIs, same convention) — both silently would have raised on a genuine no-match query instead of returning a normal empty result; fixed by checking `status_code == 404` before `raise_for_status()` in each, same pattern used for ChEMBL/Open Targets-style empty results elsewhere. Live-verified with one combined 3-tool query on EGFR (KEGG pathways + Reactome signaling summary + STRING interaction partners): all three tool calls correctly attributed (no isolation leak), 67 grounded citations spanning all three new `RECORD_REF_PATTERNS` formats (KEGG pathway IDs, Reactome stable IDs, STRING protein IDs) added in the same pass — the KEGG pattern deliberately restricted to a lowercase-only prefix (`[a-z]{2,4}\d{5}`) since a broader `\w`-based version would false-match digit substrings already claimed by the PMID/ChEMBL patterns.
- [~] **ClinicalTrials.gov/DailyMed/PharmGKB** (Shortlist #6) — ClinicalTrials.gov and DailyMed done and live-verified 2026-08-15, paired with Phase 4's review-marker convention as intended (built together, not sequenced separately). **PharmGKB not done** — it rebranded to ClinPGx and its old public API (`api.pharmgkb.org/v1/data/*`) no longer resolves; every guessed replacement path on `clinpgx.org` returns HTTP 200 with the SPA's HTML shell rather than JSON or a clean 404, so there's no reliable way to find the real API surface by trial and error — needs actual ClinPGx API docs before this can be picked back up.
  - **The review-marker convention itself (Phase 4 item, pulled forward)** — `ToolSource.requires_expert_review` (new column, migration `64c84f9901a8`) is an explicit per-source flag, not derived from `category`; `Response.requires_expert_review` is computed once at creation from whether any surviving citation traces back to a flagged source (`app/routers/mattermost_webhook.py`), and stored rather than recomputed on read, same precedent as `provenance_type`. `MattermostClient.post_message` now accepts `attachments`; a flagged response posts with a reserved color bar (`#D72638`, `EXPERT_REVIEW_COLOR` — reserved only for this, per the UX spec) and a "⚠️ Requires expert review" pretext. **Retroactively fixed ClinVar**, which the UX doc names explicitly ("clinical variant databases") but had been wired earlier this session before this convention existed — `seed_dev_data.py`'s `KNOWN_TOOL_SOURCES` now carries the flag per source and the script updates an already-created `ToolSource` row if the flag changed, not just at creation time. Verified in both directions on live Mattermost posts: a clinical-trials+drug-label query produced `requires_expert_review=True` with the real color-bar attachment confirmed via the Mattermost REST API on the actual posted message; a PubMed-only control query produced `False` with no attachment — no false positive.
  - `app/tools/clinicaltrials.py` (`search_trials`, ClinicalTrials.gov REST API v2) and `app/tools/dailymed.py` (`search_drug_labels`, NLM DailyMed REST API) — both free/unauthenticated. Added `ClinicalTrials.gov ID {}` (`NCT\d{8}`) and `DailyMed set ID {}` (UUID format) to `RECORD_REF_PATTERNS`.
- [x] **PDB/AlphaFold DB** (Shortlist #8) — done and live-verified 2026-08-15, scoped strictly to structure *lookup* as planned (Gap 7/compute stays deferred to Phase 6 — no docking/folding inference here). `app/tools/pdb.py` (`search_structures`, RCSB full-text Search API + a GraphQL batch call for title/resolution/method — experimentally determined structures) and `app/tools/alphafold.py` (`get_predicted_structure`, EBI's AlphaFold DB API by UniProt accession — predicted structures with pLDDT confidence breakdown), both free/unauthenticated. Found and fixed two empty-result edge cases before the live test: RCSB's search returns 204 (not an empty result list) for zero matches, and AlphaFold DB returns 400 (not 404) for an unrecognized accession — same "non-obvious status code for a normal empty result" pattern as Reactome/STRING earlier in this phase, now four APIs deep. **Added a PDB ID citation pattern with a genuine precision fix**: a bare 4-character PDB ID (digit + 3 alphanumeric) is too ambiguous to match standalone — it would false-match years, taxon IDs, and other incidental digit-led tokens already claimed by other tools' result text — so the regex requires the literal `PDB ` immediately before it (a lookbehind), scoping extraction to how `pdb.py` actually formats its own output rather than matching the bare pattern anywhere. Live-verified with a combined PDB+AlphaFold query on EGFR: both tools correctly attributed (no isolation leak), 20 grounded citations spanning PDB IDs, the AlphaFold model ID, and a cross-matched UniProt accession from an earlier pattern; `requires_expert_review` correctly stayed `False` (structural biology isn't clinical/regulatory-sensitive).
- [~] First BYO-credentialed tool (DrugBank) — **placeholder created 2026-08-16 per user decision ("solve for this later")**: a `ToolSource` row for `drugbank` exists (`commercial_license`, `requires_credential=True`) and is bound to the agent, but there's no `app/tools/drugbank.py` and no `TOOL_BUILDERS` entry in `app/tool_roster.py`, so it's inert — `build_tool_roster()` silently skips any bound `ToolSource` with no matching builder (the same mechanism that already protects against unwired-but-cataloged sources), confirmed live: the roster still resolves to exactly the 16 real tool sources with `drugbank` absent. `scripts/seed_dev_data.py`'s `KNOWN_TOOL_SOURCES` tuple extended with a `requires_credential` field to support this. Actually exercising the Credential Vault (`app/vault.py`, built in Phase 0) for real needs a DrugBank API key from the user before any of `compound_search`/whatever DrugBank's real endpoints turn out to be can be built and tested.

**Exit criterion — met 2026-08-15, for the 3 tools wired so far.** Live-tested with a genuine 3-tool query ("EGFR as a cancer target — check Open Targets for disease associations, then ChEMBL for known active compounds, then a supporting PubMed review"): the agent posted a 3-step plan naming all three tools before executing any of them, made 7 real tool calls across open_targets/chembl/pubmed, and synthesized one grounded report with 21 citations spanning all three record-ID formats correctly. This is the real proof of the "puzzle pieces assembled per request" model, not a written scenario. **Roster status update, same day:** Literature Discovery & Acquisition, Ensembl, UniProt, ClinVar, gnomAD, and Ontologies are now also done and live-verified (9 tool sources total), each following this same one-at-a-time build→standalone-test→wire→seed→live-verify→commit cycle. Remaining: RxDis (blocked on its own Supabase + heavy ML deps, deliberately deferred), KEGG/Reactome/STRING, ClinicalTrials.gov/DailyMed/PharmGKB, PDB/AlphaFold DB, and the first BYO-credentialed tool.

## Phase 4 — Grounding hardening + first regulatory-adjacent flag

- [x] Message-attachment structured-output rendering (UX Behavior §3) — done and live-verified 2026-08-15. `app/output_rendering.py`'s `build_response_attachment()`: counts markdown table rows in the body (a plain `|...|` line count, excluding the `|---|---|` separator); a body with ≤5 such rows (roughly a small table including its header, or no table at all) renders in full inside the attachment, same as before; a larger table gets truncated to the first ~600 characters plus a "📄 Full report" link. The link points at a new `GET /reports/{response_id}` (`app/routers/reports.py`), which serves the Response row's full `body` as plain `text/markdown` — no canvas view, no markdown-to-HTML rendering dependency, exactly the MVP scope the UX doc calls for. New `ORCHESTRATOR_PUBLIC_URL` setting (defaults to `http://localhost:8000`) since this link is clicked from a human's own browser, not reached container-to-container like `MATTERMOST_URL` is. Every response now posts with `message=""` and the real content entirely in `attachments[0].text` (previously a plain-text message, no attachment at all) — confirmed Mattermost accepts an empty top-level message when attachments carry the content. Live-verified both paths on real Mattermost posts fetched via the REST API: a 15-row PubMed table truncated correctly with a working report link (fetched the full 3201-character body back from `/reports/{id}`, confirmed 404 on an unknown ID); a 3-row table rendered in full with no link.
- [x] The reserved "requires expert review" visual marker (UX Behavior §4) — done 2026-08-15, built alongside ClinicalTrials.gov/DailyMed (see Shortlist #6 above) rather than sequenced after, per this bullet's own original instruction. Full detail there; summary: `ToolSource.requires_expert_review` is explicit per-source, `Response.requires_expert_review` is derived once from which cited tool calls trace back to a flagged source, a flagged response posts with a reserved color-bar attachment (`#D72638`) — verified live in both directions (true with the real attachment confirmed via the Mattermost API; false with no attachment on a control query), and retroactively applied to ClinVar.
- [x] `#grounding-log` audit channel (FR-10) — done and live-verified 2026-08-15. New `Org.grounding_log_channel_id` (migration `e14bb9ce43aa`) — the human-facing surface of the `TOOL_CALL` table, org-scoped since audit logging isn't agent-specific. `scripts/bootstrap_mattermost.sh` now creates the `#grounding-log` channel (public, bot auto-joined) on a fresh install and prints its ID for `seed_dev_data.py --grounding-log-channel-id`, which also updates an existing Org's channel ID on reseed. `mattermost_webhook.py` posts a compact summary (task ID, provenance, tool sources used, citation record refs, and the review-required flag when set) to that channel after every response, skipped entirely if the org hasn't configured one. Live-verified with two real posts: a PubMed-only query logged `provenance: grounded`, no review flag, 10 citations; a ClinicalTrials.gov query logged the same plus `⚠️ requires expert review` — both fetched directly from the real channel via the Mattermost REST API, not inferred from code review.

**Exit criterion — met 2026-08-15, all three bullets done.** AC-9 passing (verified live, not just documented): the visual-marker convention is enforced in code, keyed on grounding source via `ToolSource.requires_expert_review`, not agent identity — there's only one agent, so this was never at risk of being confused with per-agent routing. Phase 4 is fully done.

## Phase 5 — Continue growing the roster: remaining Shortlist items + overlooked resources

- [x] Multi-DB variant consensus capability (Shortlist #7) — proven 2026-08-15, and needed no new tool source: Ensembl, UniProt, ClinVar, and gnomAD were already wired in Phase 3, so this was purely a question of whether the master agent could actually assemble them into one consensus view on its own, which is the whole point of the architecture. Live-tested with a genuine cross-database BRCA1 query (Ensembl gene ID/location + UniProt function + a ClinVar pathogenic variant + that variant's gnomAD population frequency). **First attempt surfaced two real, honestly-handled gaps rather than fabricated output** (took ~20 minutes — the run was legitimately working, not stuck, confirmed by watching the `claude` subprocess stay alive and periodically spawn new child processes): (1) `rest.ensembl.org` was down again (a recurring external instability, not new — see Ensembl's Shortlist #2 entry above), retried 4 times and correctly reported as unavailable rather than guessing a gene ID from memory; (2) gnomAD's `get_variant_frequency` needs an exact GRCh38 chrom-pos-ref-alt ID, and ClinVar's tool only returned HGVS/cDNA notation at the time, so the model correctly said it couldn't chain the two rather than inventing a coordinate. **Fixed the second gap**: `app/tools/clinvar.py`'s `search_variants` now also surfaces each variant's GRCh38 chr/start/stop from `variation_set[0].variation_loc` (not a full ref/alt-ready gnomAD ID -- indels don't map to a single ref/alt pair from a coordinate range alone, so full automatic chaining for indels is still a known limitation, not attempted). Re-verified live and fast (~3 min, no Ensembl dependency this time): a targeted ClinVar query now returns real GRCh38 coordinates inline, grounded, 7 citations.
- [x] Corpus↔OA cross-reference (Shortlist #10) — done and live-verified 2026-08-16. The compliance boundary was previously enforced only by convention (the system prompt telling the model to check `discover_papers`'s OA status before calling `check_scihub_availability`) — real, but not structurally guaranteed if a call site skipped that step. `check_scihub_availability` in `app/tools/literature_discovery.py` now re-checks each DOI's OpenAlex open-access status itself (one batched `filter=doi:a|b|c` request, not one call per DOI) before ever consulting the local Sci-Hub index, and reports the legal OA link instead whenever one exists — regardless of whether the caller already checked. This is the actual fix Gap 9 needs: the corpus itself (the flat 88.3M-line Sci-Hub DOI list) carries no OA/licensing metadata of its own, so it can never be trusted alone to decide availability. Live-verified by calling the tool directly, bypassing `discover_papers` entirely, on one genuinely-OA DOI and one Sci-Hub-only DOI: the OA one correctly came back "prefer this over Sci-Hub" with the real publisher link, the other correctly came back Sci-Hub-tier -- the boundary held even when the documented two-step waterfall wasn't followed.
- [~] **Hugging Face** — plumbing done 2026-08-16, live verification pending a real API token. `app/tools/huggingface.py`'s `predict_masked_residue` (ESM2 protein language model, fill-mask via `router.huggingface.co` -- the old unauthenticated `api-inference.huggingface.co` endpoint is dead, confirmed by DNS failure). **First BYO-credential tool actually wired end-to-end**, not just a placeholder: `app/tool_roster.py`'s `build_tool_roster` now branches on a `CREDENTIAL_BUILDERS` set, looks up that org's `Credential` row, decrypts it via `app/vault.py`, and passes it into the builder -- a tool source with `requires_credential=True` but no credential configured yet stays bound-but-inert, same mechanism as the DrugBank placeholder. **Also built the missing piece that made this possible**: `scripts/add_credential.py` didn't exist before -- there was no path for anyone to actually add a BYO credential despite the Credential model and vault existing since Phase 0. Product requirement per the user (2026-08-16): built generically for any org's own token, not hardcoded to one person's key.
- [x] **bio.tools + GitHub-repo triage (Shortlist #9, generalized beyond just Immunoinformatics to all 19 categories per explicit user follow-up)** — done 2026-08-16. First pass checked 253 Python library/CLI candidates across Cheminformatics + Immunoinformatics; user then asked whether this covered *all* the Vault's bio.tools/GitHub content, not just those two categories, so the same triage was re-run across all 19 categories (33,888 entries total). **Confirmed the pattern generalizes**: 4,397 Python library/CLI candidates system-wide, but only 1,000 entries anywhere have a real star count -- and those 1,000 turned out to be the exact same GitHub Topic Index already scanned in the first pass (cross-tagged into categories by domain, not a separate signal). The other ~32,888 entries are bio.tools-sourced with `stars: null` everywhere, the same single-paper-research-code pattern regardless of category. So the first pass's signal was already complete, not narrow — this was a real verification, not just an assumption. From the full star-ranked list, picked two general-purpose, actively-maintained, cleanly pip-installable packages: **scikit-bio** (969+ stars) and **BioPandas** (756+ stars), both resolved with prebuilt wheels via `uv pip install --dry-run` (39 total packages, no GPU deps, no source compilation) before actually installing. Also picked a third after re-evaluating a package initially deferred as needing "a model-sourcing story": **cobrapy** (579+ stars) -- found a real, standard public source for genome-scale metabolic models (BiGG Models, bigg.ucsd.edu, 108 published models) that makes it genuinely usable, not just installable. `app/tools/scikit_bio.py`'s `compute_diversity_metrics` (Shannon/Simpson/richness on a taxon abundance table) fills a domain this platform had **zero** coverage in (microbiome/community ecology). `app/tools/biopandas_structure.py`'s `get_structure_composition` (chains/residues/bound ligands for a real PDB structure file) extends `pdb.py` past search metadata into actual structural content. `app/tools/cobra_fba.py`'s `search_metabolic_models` + `run_flux_balance_analysis` *simulates* a metabolic network (predicted growth rate, flux distribution) -- a genuinely different capability from KEGG/Reactome's static pathway *lookup*. **All three are the first tools in this roster that run a real local computation in-process rather than calling an external API** -- no network dependency for the computation itself, no external-outage risk (proof the "many small tools" architecture also works for wrapped libraries, not just REST APIs, ahead of Phase 6's harder sandboxed-runner problem for CLI-only tools). **Found and fixed a real dependency conflict before installing cobra**: it requires `pandas<3.0`, but scikit-bio/BioPandas had already pulled in pandas 3.0.5 -- checked both existing packages' actual constraints (neither pins an upper bound) before downgrading, then ran a standalone regression check on both after the downgrade to confirm nothing broke, rather than assuming compatibility. **Found and fixed a real under-citation bug on the first live test**: the model computed all 3 diversity metrics correctly but dropped 2 of 3 `[scikit-bio:metric]` citation tags when writing its answer table, paraphrasing them into plain labels instead -- exact same failure class as Phase 3's Open Targets under-citation bug, fixed the same way (tightened the system prompt to require the bracketed tag be copied verbatim, not paraphrased). Re-verified live: 1/3 citations before the fix, 3/3 after, on the same query; cobra's citations worked correctly on the first live test with no fix needed. All three tools live-verified with correct attribution and no isolation leaks. **Found and fixed a separate, pre-existing "anyone can clone this" bug while touching dependency files**: `claude_agent_sdk` itself was never declared in `pyproject.toml`/`requirements.txt` despite being required at import time -- a fresh clone following the README would have crashed immediately. Fixed alongside adding scikit-bio/BioPandas/cobra as declared dependencies.

- [x] **AutoDock Vina docking (a fourth bio.tools/GitHub-triage tool, added after direct user follow-up)** — done and live-verified 2026-08-16. User specifically flagged Jupyter_Dock (286 stars, Drug-Discovery category) as a tool they suspected was valuable and asked to make sure every candidate was actually being evaluated properly, not just the highest-starred general libraries. Jupyter_Dock itself turned out to be just notebooks; what it actually wraps is **AutoDock Vina** and LeDock -- classical, physics-based, CPU-only docking engines, genuinely different from the GPU/model-weights-heavy deep-learning docking tools (DiffDock etc.) that had been correctly excluded earlier -- the two were wrongly bucketed together on a first pass without checking what each one actually depends on. This is the single most consequential capability this phase added: the platform had ChEMBL (known bioactivity) and PDB (structure lookup) but **nothing that actually predicts how well a candidate molecule binds a target structure** -- the literal core computational step of structure-based drug discovery, and the report's own flagship 5.5 ("Target-to-Lead Virtual Screening Funnel"). `app/tools/vina_docking.py`'s `dock_ligand`: fetches a receptor from RCSB, auto-detects the binding site from a co-crystallized ligand/cofactor (or an explicit one), builds a 3D ligand conformer from SMILES via RDKit, preps both sides as PDBQT via Meeko, and docks with the real `vina` Python bindings -- all CPU-bound work via `asyncio.to_thread` so it doesn't block the event loop, real dock completes in ~10-20s. Getting the actual pipeline working took real debugging, not just installing packages: meeko's receptor-loading API needed several undocumented calls worked out from source inspection (`Polymer.from_pdb_string` needs explicit `chem_templates`/`mk_prep` args and `allow_bad_res`/`default_altloc` to handle real crystal structures' altloc residues and the co-crystallized ligand itself blocking template matching), and Vina's `score()`/`optimize()` need a pre-positioned pose while `dock()` performs its own search within the box -- calling the wrong one first produced a real, diagnosable "ligand outside grid box" error, not a silent wrong answer. **Found and fixed a real dependency-declaration gap**: `meeko` imports `gemmi` at module load time in a submodule but doesn't declare it as a dependency, so `import meeko` crashed until `gemmi` was added explicitly -- not caught by the dry-run resolver, only by actually importing. Live-verified end-to-end (erlotinib vs. the EGFR kinase domain, PDB 1M17): correctly auto-detected the co-crystallized AQ4 (erlotinib) ligand to center the search, real binding affinity computed (-5.8 kcal/mol range across the top 5 poses), the `[vina:1M17]` citation tag preserved verbatim in the final answer (no under-citation this time), grounded, no isolation leak, and the model added an honest, scientifically appropriate caveat about Vina scores being relative rather than a direct Kd/IC50 proxy rather than overstating the result.

**Exit criterion:** the roster covers a genuinely broad spread of the research report's Section 4 clusters — the point being to prove growing the roster is actually low-friction per the Section 7 wrapping pattern, not that any specific tool count is hit. **Substantially met 2026-08-16**: 20 tool sources now live-verified (16 external-API + 4 wrapped-local-library tools), spanning literature, drug discovery, genomics, ontologies, systems biology, clinical/regulatory, structural biology, and microbiome -- and, as of the Vina docking tool, actual structure-based binding-affinity prediction, not just data lookup. Hugging Face is wired but not yet live-verified (pending a real token); DrugBank is an inert placeholder (pending a paid credential).

## Phase 6 — Compute layer (Gap 7) and the sandboxed tool-runner (Section 7)

Section 7's actual strategy, summarized (full detail in the report, not reproduced here): API-callable tool sources wrap cheaply and need no compute of their own; local CLI/binary tool sources (the majority of the 33,110-entry bio.tools catalog) need a sandboxed execution environment, which is the harder half of this phase. The report's own phasing: (1) wrap the highest-experiment-density, permissively-licensed, API-callable subset first — Immunoinformatics and Cheminformatics; (2) build the sandbox before touching Structural-Biology/Molecular-Dynamics, which get zero benefit from an API-only wrapper; (3) wrap the rest demand-driven, not upfront.

- [ ] Decide buy-vs-build per the research report's Section 9 feasibility rating (Tier 2, procurement blocker) for NVIDIA Platform/cloud GPU vs. the containerized tool-runner (Tier 3, compute-infra blocker) for the CLI/binary bio.tools long tail.
- [ ] First wrap of a Phase-1-priority bio.tools category (Immunoinformatics or Cheminformatics, per Section 7's prioritization table).
- [ ] License-compatibility gate (Section 7) — automated check before any GPL/AGPL-licensed wrapped tool is exposed to a commercial-tier context.

**Exit criterion:** at least one previously-uncallable bio.tools entry is a working roster addition, proving the wrapping pattern generalizes beyond the already-live MCPs.

## Ongoing, not phase-bound

- [x] Containerize the Claude Code/Codex Runner properly — done 2026-08-17. `orchestrator/Dockerfile` now installs Node.js 20 + `@anthropic-ai/claude-code` and runs as a non-root user (the CLI's `--dangerously-skip-permissions`, which `permission_mode="bypassPermissions"` maps to, refuses to run as root/UID 0 outside a declared sandbox). Supports **both** auth paths, not just API-key: `ANTHROPIC_API_KEY` env var (headless), or a one-time `docker compose exec orchestrator claude login` for a Claude subscription with no API key — credentials persist in the new `claude_config` named volume across restarts. This is what actually made the "someone else clones this and it just works" goal true cross-platform: previously the orchestrator only ran against a `claude` CLI already installed and authenticated on the developer's own host (confirmed via a real bug hunt 2026-08-17 — the orchestrator had drifted to `--host 127.0.0.1`, unreachable from the Mattermost container's `host.docker.internal` callback, silently breaking the whole `@orchestrator` flow). Also replaced `scripts/bootstrap_mattermost.sh` (bash+curl+jq, POSIX-only) with `scripts/bootstrap_mattermost.py` (stdlib-only, identical invocation on Mac/Linux/Windows) — the SDK itself refuses to run npm's Windows `claude.cmd` shim, so native Windows host execution was never going to work; running everything in Linux containers via Docker Desktop sidesteps that entirely.
- Update `researcher-lab-experiment-catalog-2026-08-15.md`'s gap analysis once enough of Phase 3/5's roster growth confirms how much of the report's Tier-2 ratings actually upgrade to Tier-1 given `data/Databases/`'s existing local holdings.
- `CHANGELOG.md` (repo root) — every phase completion and every architecture decision reversal gets an entry.
- The auto-memory project file — kept current with what's actually built vs. planned.

## Related documents

All of `docs/` — this is the plan that ties them together. `11-backlog-traceability.md` for the full report-to-phase mapping. Primary upstream source: [[researcher-lab-experiment-catalog-2026-08-15]].
