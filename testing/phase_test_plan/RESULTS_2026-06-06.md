# Test Results — 2026-06-06

**Agents run:** 3 (API Contracts, UI Walkthrough, Regression Checklist)
**Backend:** http://localhost:8000 ✓ running
**Frontend:** http://localhost:5173 ✓ running
**LM Studio:** ✗ NOT running (localhost:1234 offline — blocks new runs from starting Phase 1)

---

## Regression Checklist — 14/14 PASS

| # | Check | Result |
|---|-------|--------|
| 1 | GET /api/health → 200 | PASS |
| 2 | POST standard fixture → 200 | PASS |
| 3 | TypeScript `npx tsc --noEmit` → 0 errors | PASS |
| 4 | INDICATIONS = ['oncology','chronic','acute'] | PASS |
| 5 | provider=lmstudio + oncology → 200 | PASS |
| 6 | provider=lmstudio + chronic → 200 | PASS |
| 7 | provider=lmstudio + acute → 200 | PASS |
| 8 | provider=local → 422 (expected rejection) | PASS |
| 9 | indication_type=neurology → 422 (expected rejection) | PASS |
| 10 | store.ts IMPLEMENTED_MAX = 9 | PASS |
| 11 | RunCanvas.tsx isImplemented: phaseNum <= 9 | PASS |
| 12 | E2EConfig.tsx provider='lmstudio', INDICATIONS correct, pu_n_bags=30 | PASS |
| 13 | Sidebar.tsx RunRow has onClick prop | PASS |
| 14 | runner.py writes both xgb_probability and pu_bio_score | PASS |

---

## API Contract Tests — 14 PASS / 2 FAIL / 1 BLOCKED

### Route-Level Contracts

| Test | Expected | Actual | Result |
|------|----------|--------|--------|
| GET /api/health | 200, status:ok, supabase:true, 10 phases | ✓ | PASS |
| POST /api/runs (standard fixture) | 200, run_id UUID | ✓ | PASS |
| POST /api/runs provider:"local" | 422 | 422 "Input should be 'lmstudio', 'anthropic' or 'openai'" | PASS |
| POST /api/runs indication_type:"neurology" | 422 | 422 "Input should be 'chronic', 'acute' or 'oncology'" | PASS |
| GET /api/runs | 200, runs array, phase_names | ✓ 6 runs, keys 0–9 | PASS |
| GET /api/runs/{id} | 200, status field, phases array | ✓ | PASS |
| GET /api/runs/{id}/targets | 200, array | ✓ | PASS |
| GET /api/runs/{id}/compute | 200, compute array | ✓ | PASS |
| GET /api/runs/{id}/events | 200, events array, done field | ✓ 18 events | PASS |
| GET /api/system/telemetry | 200, ram_used_gb, cpu_pct | ✓ | PASS |
| GET /api/genes?q=KRAS | 200, results includes "KRAS" | ✓ | PASS |
| GET /api/genes?q= (empty) | 200, results:[] | ✓ | PASS |
| GET /api/runs/{wrong_id} | 404 | ✓ "run not found" | PASS |
| POST /api/module-runs | 501 | ✓ "Single-module runs not supported" | PASS |
| **POST /api/runs disease:""** | **422** | **400** "disease is required" | **FAIL** |
| **POST /api/runs known_positives:[]** | **422** | **400** "At least one known positive gene" | **FAIL** |
| Phase 1 full run + xgb_probability check | ≥10 targets, xgb_probability non-null | BLOCKED — LM Studio offline; Phase 0 returns go_no_go:no_go | BLOCKED |

### Phase Data Checks (using existing Parkinson's completed run)

| Check | Result | Detail |
|-------|--------|--------|
| Phase 0 events — DB files present | PASS | PrimeKG, DepMap, STRING, ChEMBL all found |
| Phase 1 — known positives in target list | PASS | LRRK2, SNCA, PINK1, PRKN, GBA all present |
| Phase 1 — ranks sequential | PASS | 1–22 unique ranks |
| Phase 1 — duplicate rows | **FAIL (NEW-B02)** | 42 rows returned, only 22 unique symbols |
| Phase 1 — xgb_probability on old run | FAIL (pre-fix, expected) | NULL — run predates BUG-F06 fix |
| Phase 2 — evidence_trail.phase2 block | **FAIL (NEW-B03)** | NULL on ALL 42 targets despite Phase 2 "completed" + 459s runtime |
| Phase 2 — validation_score persisted | **FAIL (NEW-B03)** | NULL on ALL targets |
| GET /api/events after server restart | **FAIL (NEW-B04)** | events:[] — in-memory hub evicted, no DB persistence |

---

## UI Walkthrough — Code Verification

### Bug Fix Verification

| Bug | Status | Evidence |
|-----|--------|---------|
| BUG-F01 — provider:'lmstudio' | PASS | E2EConfig.tsx line 113 |
| BUG-F02 — INDICATIONS correct | PASS | E2EConfig.tsx line 40 |
| BUG-F03 — IMPLEMENTED_MAX=9 | PASS | store.ts line 60 |
| BUG-F04 — isImplemented phaseNum<=9 | PASS | RunCanvas.tsx line 18 |
| BUG-F05 — no auto-jump to phaseView:1 | **FAIL** | store.ts line 385: `if (!phaseView) phaseView = 1;` still present in SSE `run:completed` handler — SSE replay on sidebar click re-triggers it |
| BUG-F06 — xgb_probability written by runner | PASS (code) | runner.py lines 261, 313 — both keys written |
| BUG-F06 — xgb_probability in API response | **FAIL (NEW-B06)** | Live API response for `/runs/{id}/targets` does NOT include `xgb_probability` in evidence_trail — serialiser omits it. `isSeeded` check in RunCanvas.tsx line 369 always false → ★ star never renders |
| BUG-F07 — Sidebar RunRow onClick | PASS | Sidebar.tsx line 148 |
| BUG-F08 — pu_n_bags=30 | PASS | E2EConfig.tsx line 115 |

### Section Coverage

| Section | Outcome |
|---------|---------|
| Auth flow | CANNOT_VERIFY (browser required) |
| Home dashboard | CANNOT_VERIFY (browser required) |
| E2EConfig — indication/provider/pu_n_bags | PASS (code verified) |
| RunCanvas — phases 5–9 not greyed out | PASS (IMPLEMENTED_MAX=9 confirmed) |
| RunCanvas — completed run no auto-jump | FAIL (BUG-F05 still in SSE handler) |
| Phase 1 — PU score column populated | PASS (pu_bio_score in API response confirmed) |
| Phase 1 — ★ star for known positives | FAIL (xgb_probability missing from API response) |
| Sidebar — RunRow clickable | PASS (onClick wired) |
| Sidebar — new run appears after launch | PASS (loadRuns() called after startRun) |

---

## All Bugs — Consolidated

### Still Open (unfixed)

| ID | Sev | File | Description | Fix |
|----|-----|------|-------------|-----|
| BUG-001 | MED | store.ts, Sidebar.tsx | `railOpen` shared by sidebar + AI rail | Split into `sidebarExpanded` + `railOpen` |
| BUG-002 | LOW | E2EConfig.tsx | Gene search uses 10-gene mock | Wire to `/api/genes?q=` |
| BUG-003 | LOW | E2EConfig.tsx | SYS_CHECKS hardcoded | Call `/api/health` on mount |
| BUG-004 | LOW | E2EConfig.tsx | EFO lookup fake | Wire to real EFO/OT lookup |
| BUG-005 | LOW | Phase 7 UI | Stop early / Run more are no-ops | Backend signal endpoint needed |
| BUG-006 | LOW | Phase 9 UI | Download ZIP no-op | `GET /api/runs/{id}/package` endpoint |
| BUG-007 | LOW | schema.sql | `seeded` column not persisted to DB | `ALTER TABLE targets ADD COLUMN seeded boolean DEFAULT false` |

### Newly Found This Session

| ID | Sev | File | Description | Fix |
|----|-----|------|-------------|-----|
| NEW-B01 | LOW | src/api/main.py | Empty `disease` or `known_positives:[]` returns HTTP 400 instead of 422 | Move validation into Pydantic `RunRequest` model with `@field_validator` |
| NEW-B02 | HIGH | src/phases/phase1/runner.py | Duplicate target rows (42 rows, 22 unique symbols) in Parkinson's run | `clear_targets` before Phase 1 upsert, or add unique constraint on `(run_id, symbol)` |
| NEW-B03 | HIGH | src/phases/phase2/runner.py | Phase 2 marks "completed" but `validation_score` and `evidence_trail.phase2` are NULL on all targets | DB write silently failing — add error logging around upsert calls |
| NEW-B04 | MED | src/api/events.py | `/events` returns `[]` after server restart — EventHub in-memory, no DB persistence | Persist events to `events` table and replay from DB |
| NEW-B05 | MED | src/api/orchestrator.py | LLM offline → `go_no_go:no_go` blocks entire run; spec says it should warn but continue | Make LLM check non-blocking (warn only) or document as intentional |
| NEW-B06 | HIGH | src/api/main.py `_get_targets()` | `xgb_probability` written to DB by runner but missing from API response — ★ stars never render | Add `xgb_probability` to the SELECT or evidence_trail serialisation in `_get_targets()` |

### Previously Fixed (all confirmed)

| ID | Fix Confirmed |
|----|--------------|
| BUG-F01 | ✓ provider:'lmstudio' |
| BUG-F02 | ✓ INDICATIONS correct |
| BUG-F03 | ✓ IMPLEMENTED_MAX=9 |
| BUG-F04 | ✓ isImplemented phaseNum<=9 |
| BUG-F05 | ✗ NOT fixed — SSE handler still sets phaseView=1 |
| BUG-F06 | ✓ runner writes xgb_probability; ✗ API doesn't return it |
| BUG-F07 | ✓ Sidebar onClick wired |
| BUG-F08 | ✓ pu_n_bags=30 |

---

## Priority Fix List

1. **NEW-B03** (HIGH) — Phase 2 silent data loss: `evidence_trail.phase2` not persisted. Run any Phase 2 and get nothing back.
2. **NEW-B06** (HIGH) — `xgb_probability` missing from API response. Fix: add to SELECT in `_get_targets()`.
3. **BUG-F05** (MED) — SSE replay auto-jumps to Phase 1 on sidebar open. Fix: remove `if (!phaseView) phaseView = 1` from `_applyBatch`.
4. **NEW-B02** (HIGH) — Duplicate target rows. Fix: add unique constraint `(run_id, symbol)` on targets table.
5. **NEW-B04** (MED) — Events lost after server restart. Fix: persist to DB.
6. **NEW-B01** (LOW) — 400 vs 422 on empty inputs. Fix: Pydantic validator.
7. **LM Studio** — Start it before running any new end-to-end test.
