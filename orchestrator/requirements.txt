# Quick-install path (pip install -r requirements.txt). pyproject.toml is the
# source of truth for version ranges -- keep these in sync if either changes.
fastapi>=0.115,<1.0
python-multipart>=0.0.12,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
cryptography>=43.0,<44.0
claude-agent-sdk>=0.2.139,<1.0
pymupdf>=1.24,<2.0
anthropic>=0.40,<1.0

# Wrapped, in-process bio libraries (docs/10-build-plan.md Phase 5's
# bio.tools/GitHub triage)
scikit-bio>=0.7,<1.0
biopandas>=0.5,<1.0
cobra>=0.32,<1.0
rdkit>=2026.3,<2027.0
vina>=1.2,<2.0
meeko>=0.7,<1.0
gemmi>=0.7,<1.0
primer3-py>=2.0,<3.0
pyhmmer>=0.12,<1.0
msprime>=1.3,<2.0
plip>=3.0,<4.0
# mhcflurry and soltrannet depend on torch -- install CPU-only torch FIRST
# to avoid pulling the full CUDA toolkit:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# mhcflurry also needs a one-time model download after installing:
#   mhcflurry-downloads fetch models_class1_pan
mhcflurry>=2.0,<3.0
gseapy>=1.3,<2.0
gprofiler-official>=1.0,<2.0
pyteomics>=4.7,<5.0
piqtree>=0.7,<1.0
# Pinned explicitly: piqtree's own constraint (cogent3>=2025.7.10a8) plus
# this file's newer additions gave pip's resolver hundreds of cogent3
# pre-release candidates to backtrack through, hitting its hard
# ResolutionTooDeep(200000) limit and failing the whole build (confirmed
# live in a real `docker compose build`). uv's resolver solves the same
# requirements set instantly and picks this version -- pinning it here
# removes the backtracking search space for pip without changing what
# actually gets installed.
cogent3==2026.8.26
dendropy>=5.0,<6.0
phykit>=2.0,<3.0
sourmash>=4.8,<5.0
soltrannet>=1.0,<2.0
equilibrator-api>=0.6,<1.0
straindesign>=1.19,<2.0
nrpcalc>=1.7,<2.0
epitopepredict>=0.5,<1.0
anarci>=2026.2.13,<2027.0
tcrdist3>=0.3,<1.0
libroadrunner>=2.10,<3.0
egglib>=3.6,<4.0
mappy>=2.31,<3.0
mokapot>=0.10,<1.0
Auto3D>=3.0,<4.0
lightdock>=1.0.0rc2,<2.0
pybioportal>=1.0,<2.0
spyrmsd>=0.9,<1.0
correlationplus>=0.2,<1.0
omnipath>=1.0,<2.0
pycombat>=0.20,<1.0
scanpy>=1.11,<2.0
igraph>=1.0,<2.0
leidenalg>=0.12,<1.0
flair>=0.14,<1.0
ablang>=0.3,<1.0
pandasgwas>=1.2,<2.0
pydna>=5.5,<6.0
ldsc>=2.0.1,<3.0
dnachisel>=3.2,<4.0
bakta>=1.12,<2.0
checkm2>=1.0,<2.0
checkv>=1.1,<2.0

# dev/test
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
ruff>=0.7,<1.0
pytest-forked>=1.6,<2.0
pytest-rerunfailures>=14,<17
