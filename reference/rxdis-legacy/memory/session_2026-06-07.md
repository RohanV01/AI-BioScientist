---
name: session-2026-06-07
description: Session summary — bug fixes, EFO required, NIM/Neurosnap removed, FE deleted, next steps
metadata:
  type: project
---

## What happened this session

### Bug fixes applied (all confirmed)
- BUG-F05: SSE auto-jump to phaseView=1 removed from store.ts
- BUG-001: sidebarExpanded split from railOpen (store.ts + Sidebar.tsx)
- BUG-F06: xgb_probability written by runner — confirmed in API response
- NEW-B01: Empty disease/known_positives now return 422 (Pydantic validators)
- NEW-B03: update_target now logs rows affected — Phase 2 writes confirmed working
- NEW-B04: /events endpoint falls back to DB-synthesized events after server restart
- NEW-B05: LLM offline no longer blocks runs (soft gate, warns only)
- NEW-B06: False alarm — evidence_trail IS returned by API; was tested on pre-fix data

### EFO ID now required everywhere
- `disease_efo_id: str` required in RunRequest + RunConfig (was Optional)
- Format validated: EFO_XXXXXXX, MONDO:XXXXXXX, OMIM:XXXXXXX, etc.
- Phase 1 no longer calls LLM to disambiguate — uses EFO ID directly
- Frontend: E2EConfig + EngineRoom both have real EFO ID input fields
- Example: `"disease_efo_id": "EFO_0002618"` for pancreatic cancer

### Frontend deleted
- Entire `frontend/` directory removed
- User is building new FE in Google Stitch
- Will import Stitch export and wire to backend next session

### NIM + Neurosnap removed by user
- User deleted NIM and Neurosnap dependencies from Phase 6
- Now using local models instead
- Phase 6 runner files were modified (nim_rfdiffusion.py, neurosnap_boltzgen.py, peptide_gen.py, developability.py, runner.py)

### Testing completed
- P0: PASS
- P1: PASS (45 targets, AUROC ~0.87, xgb_probability confirmed)
- P2: PASS (9/45 passed, validation_score + phase2 block persisted)
- P3: PASS (all 9 P2-passed targets routed, modality + branches correct)
- P4: RUNS but candidates not persisted — **NEW-B07**: `candidates.target_id` is uuid in live Supabase DB but code sends text (gene symbol). Migration needed: `ALTER TABLE candidates ALTER COLUMN target_id TYPE text USING NULL;`
- P5: Transient Supabase EAGAIN on teardown (not a code bug)
- P6–P9: NOT yet tested — uvicorn --reload killed mid-run; deferred to next session

### Open bugs
- NEW-B07 (HIGH): candidates.target_id type mismatch — SQL migration needed in Supabase
- BUG-007 (LOW): seeded column not in live DB — SQL: `ALTER TABLE targets ADD COLUMN IF NOT EXISTS seeded boolean DEFAULT false;`
- BUG-002/003/004: Gene search mock, SYS_CHECKS hardcoded, EFO lookup (all frontend — will address with new FE)

## Backend testing progress (this session)
- **P4 deadlock fixed**: `ProcessPoolExecutor(fork)` → `ThreadPoolExecutor` in docking.py
- **P6 ProteinMPNN fixed**: length guard `<= 500` removed (was filtering all full-protein sequences)
- **P6 LLM fixed**: `lmstudio_provider.py` auto-boosts `max_tokens = max(requested, 16000)` for thinking models (Qwen3 needs ~5000-8000 thinking tokens before output)
- **P0-P9 all COMPLETED** in single run: `e379878f` pancreatic cancer, 12 min total

### P4-P9 results from run e379878f
- P4: 30 repurposing candidates, 35s
- P5: empty (peptide targets route to P6 not P5)  
- P6: 10 biologic candidates (NBN×5, CDH1×5, ATM=0 due to bad PDB URL `10AD.pdb`)
- P7: 40 Pareto candidates
- P8: 15 candidates passed validation
- P9: package `output/run/pancreatic_cancer_e379878f.zip`, audit clean

### Open bugs (to fix when FE arrives)
- **NEW-B07 (HIGH)**: `candidates.target_id` uuid in live DB → all candidate DB persists fail. Migration: `ALTER TABLE candidates ALTER COLUMN target_id TYPE text USING NULL;`
- **BUG-007 (LOW)**: `seeded` column missing from targets. Migration: `ALTER TABLE targets ADD COLUMN IF NOT EXISTS seeded boolean DEFAULT false;`
- **P6 ATM bad PDB**: `10AD.pdb` gives 404. ATM AlphaFold ID is Q13315.
- **P6 LLM tier**: With 16k token floor, may still not generate for very complex prompts. ProteinMPNN works as primary fallback.

## Next session plan
1. Import Google Stitch FE export into repo
2. Wire FE to backend API (/api/* endpoints)
3. Run Supabase migrations (NEW-B07 + BUG-007)
4. Full E2E test with new FE

**Why:** FE deleted to start fresh from Stitch; backend P0-P9 confirmed working end-to-end.
